
/**
 * The main of the inventory
 */
public class Main {

    /**
     * The class that starts everything and allows you to perform all the operations(add, remove, etc.)
     *
     * @param args the command line arguments
     */
    public static void main(String[] args) {

        CLI cli = new CLI();
        cli.start();

    }

}